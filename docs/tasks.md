# タスク分解: そこねこ(Phase 3: Tasks)

> Status: **T18 まで完了(2026-07-12)** / 最終更新: 2026-07-12
> 対象: `docs/spec.md`(Approved)/ `docs/plan.md` / `docs/design.md`(採用案: 1b, 1c, 1f, 1h, 1i, 1j)
> 実装は 1 タスク = 1 コミット。各タスクの Verify を通してから次へ進む。

## 凡例

- **受け入れ**: タスク完了時に満たすべき条件
- **Verify**: 確認方法(コマンド or 手動)
- **依存**: 先に完了している必要があるタスク
- **規模**: S(1-2 ファイル)/ M(3-5 ファイル)

---

## フェーズ 0: プロジェクト基盤

- [x] **T1: プロジェクト雛形とツールチェーン**
  - 内容: git init、Vite + React 19 + TypeScript 生成、Tailwind CSS v4、
    ESLint + Prettier、Vitest + React Testing Library、`docs/design.md` の
    カラートークンを `src/index.css` に定義、M PLUS Rounded 1c と lucide-react 導入
  - 受け入れ: `npm run dev / build / test / lint / format` が全て成功する。
    トークンを使ったプレースホルダー画面(クリーム地+オレンジヘッダー)が表示される
  - Verify: `npm run build && npm run test && npm run lint`
  - 依存: なし / 規模: M

- [x] **T2: Firebase SDK 初期化と Emulator Suite**
  - 内容: firebase / firebase-tools 導入、`src/lib/firebase.ts`(env 切替で
    エミュレータ接続)、`firebase.json`(Auth / Firestore エミュレータ)、
    `.env.local` の雛形(`.env.example`)、`npm run emulators` スクリプト
  - 受け入れ: `npm run emulators` で Auth + Firestore エミュレータが起動し、
    dev サーバーのアプリがエミュレータに接続する(デモプロジェクト ID で可)
  - Verify: エミュレータ UI(localhost:4000)で接続を手動確認
  - 依存: T1 / 規模: M
  - 注: 本番 Firebase プロジェクトの作成(コンソール)はユーザー作業。T20 までに必要

### チェックポイント A(基盤)
- [x] ビルド・テスト・lint がクリーン。エミュレータ起動を確認

---

## フェーズ 1: 認証と book 初期化

- [x] **T3: 認証(ログイン画面 + 認証ガード)**
  - 内容: AuthProvider(onAuthStateChanged 購読)、ログイン画面(デザイン 1j:
    Google + メール/パスワード + パスワードリセット + 新規登録)、
    未ログイン時のリダイレクトガード、ログアウト処理(設定画面は後続)
  - 受け入れ: エミュレータでメール新規登録 → ログイン → リロードしてもセッション維持。
    Google ログインボタンが動作する(エミュレータの擬似アカウント)
  - Verify: 手動(エミュレータ)+ AuthProvider の単体テスト
  - 依存: T2 / 規模: M

- [x] **T4: book 初期化とセキュリティルール**
  - 内容: 型定義(`src/types/`)、初回ログイン時の book 作成
    (`bookId = uid`、getDoc 存在確認 + create 専用トランザクション、
    決定的 ID でカテゴリシード)、`firestore.rules`
    (memberUids 制限 / create 時 `bookId == uid` / `ownerUid` `createdAt` 不変)、
    ルールテスト一式と `npm run test:rules`
  - 受け入れ: 初回ログインで book + シードカテゴリが作成され、2 回目以降は
    上書きされない(テストで検証)。他人の book の読み書きがルールテストで拒否される
  - Verify: `npm run test:rules` + 初期化ロジックの単体テスト
  - 依存: T3 / 規模: M

### チェックポイント B(認証)
- [x] 新規登録 → book 自動作成 → リロードで維持、をエミュレータで通しで確認

---

## フェーズ 2: マスタ管理

- [x] **T5: アプリシェルと Firestore 共通フック**
  - 内容: ボトムタブバー(ホーム / 記録 FAB / 比較 / 設定)+ オレンジのラウンド
    ヘッダーのレイアウト(デザイン 1b のシェル)、React Router のルート定義、
    `useCollection` / `useDoc` フック(onSnapshot 購読)
  - 受け入れ: 4 タブが空ページ間で遷移でき、アクティブタブが強調表示される。
    フックがエミュレータのデータを購読できる(テスト)
  - Verify: `npm run test` + 手動でタブ遷移確認
  - 依存: T4 / 規模: M

- [x] **T6: カテゴリ・店舗の管理 UI**
  - 内容: カテゴリ一覧・追加・編集(名前 + baseUnit 選択)、店舗一覧・追加・編集。
    設定タブ配下(デザイン 1i のリスト部)。削除は T9 で扱う
  - 受け入れ: カテゴリ(baseUnit 付き)と店舗を追加・改名でき、リアルタイムに一覧へ反映
  - Verify: コンポーネントテスト(フォームバリデーション)+ 手動確認
  - 依存: T5 / 規模: M

- [x] **T7: 商品の登録・編集**
  - 内容: 商品登録・編集フォーム(名前 + カテゴリ選択 + メモ)。
    カテゴリ変更は同一 baseUnit のカテゴリのみ許可(異なる場合はブロックして案内)
  - 受け入れ: 商品を登録・編集できる。baseUnit が異なるカテゴリへの変更が UI でブロックされる
  - Verify: コンポーネントテスト(カテゴリ変更制限)+ 手動確認
  - 依存: T6 / 規模: M

- [x] **T8: 単位換算ロジック**
  - 内容: `src/lib/units.ts`(kg→g、L→ml の換算、baseUnit ごとの許容単位、
    基準単位あたり単価の計算)と単体テスト
  - 受け入れ: 「米 5kg 1,980 円 → 0.396 円/g」等の換算がテストで検証されている。
    許容外の単位ペアはエラーを返す
  - Verify: `npm run test`(カバレッジ 80% 以上)
  - 依存: T1 / 規模: S(T6-T7 と並行可)

- [x] **T9: 削除と参照整合性**
  - 内容: 店舗・カテゴリの削除(参照中は件数提示でブロック)、
    商品削除(確認ダイアログ + priceRecords の分割バッチ削除、500 件上限対応)
  - 受け入れ: 参照中の店舗・カテゴリが削除できず件数が表示される。
    商品削除で配下の記録も消える(分割バッチのロジックにテスト)
  - Verify: `npm run test` + 手動確認
  - 依存: T7 / 規模: M

### チェックポイント C(マスタ)
- [x] カテゴリ・店舗・商品の CRUD が通しで動き、全テストがグリーン

---

## フェーズ 3: 価格記録と底値一覧(心臓部)

- [x] **T10: 底値算出ロジック**
  - 内容: `src/features/prices/bottomPrice.ts`(直近 N ヶ月フィルタ、
    特売込み/通常のみの底値、商品×店舗別底値、カテゴリ内単価ランキング)と単体テスト
  - 受け入れ: 特売込み/通常のみ・期間境界・特売しかない商品(通常のみ = null)の
    ケースがテストで検証されている
  - Verify: `npm run test`(カバレッジ 80% 以上)
  - 依存: T8 / 規模: S

- [x] **T11: 価格記録画面(電卓ファースト・デザイン 1c)**
  - 内容: 商品・店舗のチップ選択(その場追加可)、常時表示テンキー、
    内容量入力(「総量を入力」ヒント)、特売トグル、記録日。priceRecords への保存
  - 受け入れ: 記録がエミュレータに保存される。価格・数量は正の数のみ受け付ける。
    既存商品 + 既存店舗なら「商品タップ → 店舗タップ → 数値入力 → 保存」で完了する
  - Verify: コンポーネントテスト(バリデーション)+ 手動確認
  - 依存: T7, T10 / 規模: M

- [x] **T12: priceRecords のセキュリティルール**
  - 内容: `firestore.rules` に priceRecords / products / stores / categories の
    ルールを追加(memberUids 制限 + price / quantity の正数検証)、ルールテスト追加
  - 受け入れ: 負数・0 の price / quantity が拒否され、他人の book 配下への
    書き込みが拒否されることがルールテストで検証されている
  - Verify: `npm run test:rules`
  - 依存: T11 / 規模: S

- [x] **T13: ホーム(底値一覧・デザイン 1b)**
  - 内容: 統計サマリーヘッダー(登録商品・今週の記録・底値更新)、
    カテゴリ見出しごとの底値カード(底値・特売バッジ・店名・単価)、検索、
    参照切れの「(不明な店舗)」フォールバック
  - 受け入れ: 記録済みデータから商品ごとの底値・店舗・単価が表示され、
    特売由来にはバッジが付く。検索で絞り込める
  - Verify: コンポーネントテスト(表示ロジック)+ 手動確認
  - 依存: T10, T11 / 規模: M

- [x] **T14: 商品詳細(デザイン 1f)**
  - 内容: 底値ヒーロー(特売込み)+ 通常のみの底値併記(該当なしは「—」)、
    店舗別底値、履歴リスト、記録の編集・削除
  - 受け入れ: 履歴から記録を編集・削除でき、底値表示に即時反映される。
    特売しかない商品で通常のみ欄が「—」になる
  - Verify: コンポーネントテスト + 手動確認
  - 依存: T13 / 規模: M

### チェックポイント D(コアフロー)
- [ ] 「記録 → ホームで底値確認 → 詳細で修正」がエミュレータで通しで動く。
      spec の入力目標(3 タップ + 数値入力)を実機相当(スマホ幅)で確認

---

## フェーズ 4: 比較と設定

- [x] **T15: カテゴリ内比較(デザイン 1h)**
  - 内容: カテゴリ選択 → 所属商品の「基準単位あたり単価」ランキング(バー表示)
  - 受け入れ: 内容量・単位が異なる商品(240ml と 1.2L 等)が正しい順序で並ぶ
  - Verify: コンポーネントテスト + 手動確認
  - 依存: T10, T13 / 規模: M

- [x] **T16: 設定画面の仕上げ(デザイン 1i)**
  - 内容: 底値の対象期間(全期間 / 3 / 6 / 12 ヶ月)の変更、book 名の変更、
    カテゴリ・店舗管理への導線整理、ログアウト
  - 受け入れ: 期間変更がホーム・詳細の底値に反映される。設定変更が
    ルール(ownerUid 不変)に抵触しないことをルールテストで確認
  - Verify: `npm run test && npm run test:rules` + 手動確認
  - 依存: T13 / 規模: S

### チェックポイント E(全画面)
- [x] 全 6 画面が動作し、全テストがグリーン

---

## フェーズ 5: オフラインと PWA

- [x] **T17: Firestore オフライン永続化**
  - 内容: `persistentLocalCache` + `persistentMultipleTabManager` の有効化、
    オフライン時の UI(接続状態の表示、保存の楽観反映の確認)
  - 受け入れ: 機内モード(DevTools オフライン)で閲覧・記録ができ、
    復帰後にエミュレータへ同期される
  - Verify: 手動(DevTools オフライン切替で E2E 手順を実施)
  - 依存: T14 / 規模: S

- [x] **T18: PWA 化**
  - 内容: vite-plugin-pwa(manifest: 名前「そこねこ」・アイコン・テーマ色
    `#E8823C`、`registerType: 'prompt'`)、更新プロンプト UI、
    アプリシェルのプリキャッシュ
  - 受け入れ: Lighthouse の PWA インストール要件を満たす。
    新ビルド配信時に更新プロンプトが表示される
  - Verify: `npm run build && npm run preview` で手動確認(インストール・更新)
  - 依存: T17 / 規模: M

### チェックポイント F(オフライン)
- [ ] spec の手動 E2E チェックリスト(オフライン閲覧・記録・同期・インストール)を実施

---

## フェーズ 6: 本番デプロイ

- [ ] **T19: 本番 Firebase 設定とデプロイ**
  - 内容: 本番プロジェクトの設定値(`.env`)、Auth 承認済みドメイン・
    Google プロバイダ有効化(ユーザーと共同作業)、`firebase deploy`
    (Hosting + rules + indexes)、`npm run deploy` スクリプト
  - 受け入れ: 本番 URL でログイン〜記録〜底値表示が動く。ルールがデプロイ済み
  - Verify: スマホ実機で確認
  - 依存: T18 / 規模: S
  - 注: **デプロイは実施前にユーザーへ確認する**(Ask first)

- [ ] **T20: 最終受け入れ確認**
  - 内容: `docs/spec.md` の Success Criteria 全項目をスマホ実機で確認し、
    チェックを付けて spec を更新
  - 受け入れ: Success Criteria 全項目にチェックが付く
  - Verify: spec.md のチェックリスト
  - 依存: T19 / 規模: S

---

## フェーズ 7: Issue #1 カテゴリ内比較の表示改善

> 対象仕様: `docs/spec-issue1-category-comparison.md`(2026-07-14)

- [x] **T21: 全記録ランキング関数 `rankAllRecordsByUnitPrice`**
  - 内容: `src/features/prices/bottomPrice.ts` に `RankedRecord` 型と
    `rankAllRecordsByUnitPrice`(商品へ集約せず全価格記録を単価昇順で返す純粋関数)を追加。
    期間フィルタ(`filterRecords` 流用)・特売含む・単価 null は末尾・
    同一単価は記録日の新しい順。既存関数の挙動は変更しない
  - 受け入れ: 期間フィルタ / 単位換算(kg・L)/ 同一商品の複数記録 /
    null 末尾 / 記録日タイブレーク / カテゴリ外商品の記録除外、が単体テストで検証されている
  - Verify: `npm run test && npm run lint`
  - 依存: なし / 規模: S

- [x] **T22: ComparePage の全記録表示への変更**
  - 内容: `ComparePage` を `rankAllRecordsByUnitPrice` に置き換え。
    `useStores()` 結合による店舗名表示(参照切れは「(不明な店舗)」)、
    特売バッジ(`SaleBadge`)、記録日表示、上位50件切り詰め+「他 N 件」注記、
    行キーを `record.id` に変更
  - 受け入れ: 店舗名・記録日・特売バッジが表示され、51件以上で切り詰め注記が出る。
    key 重複警告が出ない(コンポーネントテスト)
  - Verify: `npm run test && npm run lint && npm run build`
  - 依存: T21 / 規模: S

- [ ] **T23: 旧 `rankByUnitPrice` の削除**
  - 内容: 未使用となった `rankByUnitPrice` / `RankedProduct` をテストごと削除
  - 受け入れ: 参照が残っていない(grep)。全テスト・lint・build が通る
  - Verify: `npm run test && npm run lint && npm run build`
  - 依存: T22 / 規模: S

---

## 並行性メモ

- T8(単位換算)は T6-T7 と並行可
- T15・T16 は互いに独立(並行可)
- それ以外は原則リスト順(依存関係順)

## 未確定・ユーザー作業

- 本番 Firebase プロジェクトの作成と設定値の提供(T19 まででよい)
- アプリアイコン(猫モチーフ)の用意: T18 で仮アイコンを生成し、差し替えは任意
