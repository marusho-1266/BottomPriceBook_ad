rules_version = '2';

// T4 で book 単位のルールを実装する。それまではすべて拒否(全開放にはしない)
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
